# ═══════════════════════════════════════════════════════════════════════════
# APPS BASIC - Package List
# Source: 0.spec/APPS.md
# Includes: min.conf + additional apps
# ═══════════════════════════════════════════════════════════════════════════

# Format: CATEGORY_DISTRO="package1 package2 ..."
# Distros: arch, fedora, debian

# ─────────────────────────────────────────────────────────────────────────────
# BASE SYSTEM (same as min)
# ─────────────────────────────────────────────────────────────────────────────
BASE_arch="base-devel git sudo which curl wget"
BASE_fedora="@development-tools git sudo which curl wget"
BASE_debian="build-essential git sudo curl wget"

# ─────────────────────────────────────────────────────────────────────────────
# SHELL & UTILS (extended)
# ─────────────────────────────────────────────────────────────────────────────
SHELL_arch="bash zsh fish coreutils findutils grep sed gawk less tree htop bc jq vim unzip xclip"
SHELL_fedora="bash zsh fish coreutils findutils grep sed gawk less tree htop bc jq vim unzip xclip"
SHELL_debian="bash zsh fish coreutils findutils grep sed gawk less tree htop bc jq vim unzip xclip"

# ─────────────────────────────────────────────────────────────────────────────
# MODERN CLI TOOLS
# ─────────────────────────────────────────────────────────────────────────────
MODERN_arch="eza bat fd ripgrep fzf zoxide starship yazi btop ncdu"
MODERN_fedora="eza bat fd-find ripgrep fzf zoxide starship yazi btop ncdu"
MODERN_debian="eza bat fd-find ripgrep fzf zoxide starship yazi btop ncdu"

# ─────────────────────────────────────────────────────────────────────────────
# FILE SYNC (from min)
# ─────────────────────────────────────────────────────────────────────────────
FILESYNC_arch="rsync rclone"
FILESYNC_fedora="rsync rclone"
FILESYNC_debian="rsync rclone"

# ─────────────────────────────────────────────────────────────────────────────
# CLOUD CLI (from min)
# ─────────────────────────────────────────────────────────────────────────────
CLOUD_arch="github-cli"
CLOUD_fedora="gh"
CLOUD_debian="gh"
# Note: oci-cli installed via pip

# ─────────────────────────────────────────────────────────────────────────────
# NETWORK
# ─────────────────────────────────────────────────────────────────────────────
NETWORK_arch="iproute2 iputils bind net-tools openssh ca-certificates mtr openbsd-netcat nmap"
NETWORK_fedora="iproute bind-utils net-tools openssh ca-certificates mtr nmap-ncat nmap"
NETWORK_debian="iproute2 iputils-ping dnsutils net-tools openssh-client ca-certificates mtr netcat-openbsd nmap"

# ─────────────────────────────────────────────────────────────────────────────
# PRIVACY & VPN
# ─────────────────────────────────────────────────────────────────────────────
PRIVACY_arch="tor torsocks dnscrypt-proxy wireguard-tools openvpn openresolv"
PRIVACY_fedora="tor torsocks dnscrypt-proxy wireguard-tools openvpn"
PRIVACY_debian="tor torsocks dnscrypt-proxy wireguard-tools openvpn openresolv"

# ─────────────────────────────────────────────────────────────────────────────
# COMPILERS & BUILD TOOLS
# ─────────────────────────────────────────────────────────────────────────────
COMPILER_arch="gcc clang cmake ninja make rustup go valgrind"
COMPILER_fedora="gcc clang cmake ninja-build make rust cargo golang valgrind"
COMPILER_debian="gcc clang cmake ninja-build make rustc cargo golang valgrind"

# ─────────────────────────────────────────────────────────────────────────────
# DOCUMENT TOOLS
# ─────────────────────────────────────────────────────────────────────────────
DOCS_arch="pandoc"
DOCS_fedora="pandoc"
DOCS_debian="pandoc"

# ─────────────────────────────────────────────────────────────────────────────
# LANGUAGES & RUNTIMES
# ─────────────────────────────────────────────────────────────────────────────
LANG_arch="nodejs pnpm python python-pip python-pipx"
LANG_fedora="nodejs pnpm python3 python3-pip pipx"
LANG_debian="nodejs npm python3 python3-pip pipx"

# ─────────────────────────────────────────────────────────────────────────────
# CONTAINERS & SANDBOXING
# ─────────────────────────────────────────────────────────────────────────────
SANDBOX_arch="docker podman firejail flatpak"
SANDBOX_fedora="docker podman firejail flatpak"
SANDBOX_debian="docker.io podman firejail flatpak"

# Additional (pip/pipx)
SANDBOX_pip="uv"

# ─────────────────────────────────────────────────────────────────────────────
# AI TOOLS (npm global)
# ─────────────────────────────────────────────────────────────────────────────
AI_npm="@anthropic-ai/claude-code @google/gemini-cli"

# ─────────────────────────────────────────────────────────────────────────────
# DESKTOP (for container with X11/VNC)
# ─────────────────────────────────────────────────────────────────────────────
DESKTOP_arch="xorg-server xorg-xinit openbox dmenu x11vnc ttf-dejavu breeze-icons"
DESKTOP_fedora="xorg-x11-server-Xorg xorg-x11-xinit openbox dmenu x11vnc dejavu-fonts-all breeze-icon-theme"
DESKTOP_debian="xorg openbox dmenu x11vnc fonts-dejavu breeze-icon-theme"

# ─────────────────────────────────────────────────────────────────────────────
# GUI APPS
# ─────────────────────────────────────────────────────────────────────────────
GUI_arch="konsole falkon dolphin okular libreoffice-fresh code virt-manager partitionmanager filelight krita"
GUI_fedora="konsole5 falkon dolphin okular libreoffice code virt-manager partitionmanager filelight krita"
GUI_debian="konsole falkon dolphin okular libreoffice code virt-manager partitionmanager filelight krita"
